"my plugin's directory {{{
set rtp+=~/vim-plugin/vim-markdown
set rtp+=~/vim-plugin/popup.vim
"}}}

so <sfile>:h/option.vim
so <sfile>:h/plugs.vim

syntax on

colors PaperColor

filetype plugin indent on
