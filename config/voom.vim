
let g:voom_tree_placement = "right"
let g:voom_tree_width = 36
let g:voom_ft_modes = {'markdown': 'markdown', 'tex': 'latex'}

let g:voom_tab_key = ''                 " 切换到Voom窗口的快捷键
let g:voom_return_key = '<return>'      " 切换到原窗口的快捷键
